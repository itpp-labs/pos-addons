<?xml version="1.0" encoding="UTF-8"?><odoo>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="empty_module.list">
      <field name="name">empty_module list</field>
      <field name="model">empty_module.empty_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="empty_module.action_window">
      <field name="name">empty_module window</field>
      <field name="res_model">empty_module.empty_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="empty_module.action_server">
      <field name="name">empty_module server</field>
      <field name="model_id" ref="model_empty_module_empty_module"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="empty_module" id="empty_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="empty_module.menu_1" parent="empty_module.menu_root"/>
    <menuitem name="Menu 2" id="empty_module.menu_2" parent="empty_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="empty_module.menu_1_list" parent="empty_module.menu_1"
              action="empty_module.action_window"/>
    <menuitem name="Server to list" id="empty_module" parent="empty_module.menu_2"
              action="empty_module.action_server"/>
    -->
</odoo>